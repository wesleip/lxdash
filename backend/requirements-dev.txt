# Development and test dependencies for the lxdash backend.
#
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# CI installs both files in one step.

-r requirements.txt

# --- Test runner --------------------------------------------------------------
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# --- HTTP client used by FastAPI TestClient ----------------------------------
# httpx is already pulled in by sse-starlette, but pin explicitly so the
# test client import path is stable across dependency updates.
httpx==0.28.0

# --- Linting and formatting ---------------------------------------------------
ruff==0.7.4

# --- Security scanning --------------------------------------------------------
bandit==1.7.10